CREACIÓN DE TABLAS-------------------------------------

CREATE TABLE docente (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    nombre_completo VARCHAR(255) NOT NULL
);

CREATE TABLE usuario (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    rol VARCHAR(20) NOT NULL, -- ROLE_RECTOR, ROLE_DOCENTE, ROLE_ESTUDIANTE
    docente_id BIGINT,
    
    FOREIGN KEY (docente_id) REFERENCES docente(id) 
);

CREATE TABLE asignatura (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(100) NOT NULL,
    descripcion VARCHAR(255),
    salon VARCHAR(10),
    hora_inicio TIME,
    hora_fin TIME,
    docente_encargado_id BIGINT,
    
    FOREIGN KEY (docente_encargado_id) REFERENCES docente(id)
);


INSERCION DE DATOS-------------------------------------

INSERT INTO docente (nombre_completo) VALUES ('Profesor Álgebra');

INSERT INTO docente (nombre_completo) VALUES ('Profesora Historia');

INSERT INTO docente (nombre_completo) VALUES ('Profesor Cálculo');

INSERT INTO usuario (username, password, rol, docente_id) 
VALUES ('rector', '$2a$10$HASH_CODIFICADO_DE_PASSWORD', 'ROLE_RECTOR', NULL);

INSERT INTO usuario (username, password, rol, docente_id) 
VALUES ('docente1', '$2a$10$HASH_CODIFICADO_DE_PASSWORD', 'ROLE_DOCENTE', 1);

INSERT INTO usuario (username, password, rol, docente_id) 
VALUES ('docente3', '$2a$10$HASH_CODIFICADO_DE_PASSWORD', 'ROLE_DOCENTE', 2);

INSERT INTO usuario (username, password, rol, docente_id) 
VALUES ('docente2', '$2a$10$HASH_CODIFICADO_DE_PASSWORD', 'ROLE_DOCENTE', 3);

INSERT INTO usuario (username, password, rol, docente_id) 
VALUES ('estudiante', '$2a$10$HASH_CODIFICADO_DE_PASSWORD', 'ROLE_ESTUDIANTE', NULL);